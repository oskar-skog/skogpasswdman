?       Improvements in HELP and About.
*       More debugging
?       Fix broken Docbook XML
*       gettext()
            *   Svenska
            *   Suomi
            *   Deutch
*       in "configure.2": dirs() should figure out more directories.
0.2     A pick() that returns a list.
*       info
*       menu
*       Fix getmoduledir(). Pick newest 2.*.
?       What should unquote(x) return if x is a string of whitespace?
